# agent.md

## 目标
本项目当前定位为：
- **短期**：智能招聘 MVP 验证平台。
- **长期**：中小企业数字员工服务平台（招聘、电商运营、财务报表等）。

后续开发必须保证：**所有新增代码都让系统更接近“可复用的数字员工平台能力”，而不是继续堆积一次性的招聘场景代码。**

---

## 当前阶段的架构判断
现有架构可继续演进，但只适合作为 **MVP 起点**，不允许继续扩大以下问题：
- 把运行状态只放在进程内存里。
- 把业务规则分散在前端、后端、数据库多处。
- 继续把运维控制、业务接口、平台配置混写在一起。
- 继续按“技术文件夹”堆代码，而不按“业务域 + 平台能力”收敛。

---

## 核心原则

### 1. 先平台能力，后场景功能
新增功能时，优先抽象为可复用能力：
- 任务定义
- 工作流执行
- 事件流 / 日志 / 截图
- 调度 / 取消 / 重试
- 集成适配器
- 租户 / 权限 / 审计

禁止只为单个招聘页面写一次性逻辑，且无法复用到未来的电商/财务场景。

### 2. 业务域与运行时分离
后续代码应逐步收敛为两层：
- **业务域层**：招聘、财务、电商等。
- **平台运行时层**：workflow runtime、agent runtime、integration adapters、audit/logging。

禁止把“招聘业务字段处理”直接写进通用运行时。

### 3. 写操作逐步收口到后端
后续新增的**业务写操作**默认放在后端应用服务中，不再优先新增前端直连数据库写入。

可以保留少量前端直读能力，但新增能力应优先满足：
- 规则统一
- 审计可追踪
- 权限可控制
- 后续可迁移到平台 API

### 4. 工作流必须可持久化、可恢复、可审计
所有新增工作流相关代码，都要朝以下方向演进：
- 执行状态可持久化
- 关键事件可回放
- 取消/失败可追踪
- 结果可审计

禁止继续扩大“仅内存任务状态”的依赖范围。

### 5. 配置与运维能力不得继续侵入业务代码
模型配置、Agent 配置、容器控制、部署脚本属于**平台运维能力**。

新增代码时：
- 不要把更多运维逻辑塞进业务 router。
- 不要把高权限控制能力直接暴露给普通业务流程。
- 不要把单机/单容器假设写死到业务逻辑里。

### 6. 优先模块化单体，不盲目微服务
在产品尚未稳定前，继续采用 **模块化单体** 演进：
- 同仓库可接受。
- 同后端可接受。
- 但模块边界必须清晰。

禁止为了“看起来先进”而拆微服务；也禁止继续无边界地往现有目录堆逻辑。

---

## 目录与模块演进约束
后续新增代码时，应优先向以下结构靠拢：
- `domains/recruiting/`
- `domains/ecommerce/`
- `domains/finance/`
- `platform/workflow_runtime/`
- `platform/agent_runtime/`
- `platform/integrations/`
- `platform/tenant_auth/`
- `platform/audit/`

如果暂时不重构目录，也必须遵守同样的模块边界，避免继续形成“大一统 backend/workflows 杂糅中心”。

---

## 新增代码前必须自检
每次新增功能前，开发者必须先问 5 个问题：
1. 这是招聘特有逻辑，还是未来多个数字员工场景都能复用？
2. 这段能力应该放在业务域，还是平台运行时？
3. 这次新增是否扩大了前端直写数据库的范围？
4. 这次新增是否继续依赖内存态、单机态、人工运维态？
5. 这次新增是否让未来的审计、恢复、权限控制更困难？

如果答案偏负面，应先调整设计再开发。

---

## 明确禁止事项
禁止继续新增以下类型的实现：
- 仅服务单个页面、不可复用的 workflow 特判代码。
- 把租户、权限、审计规则散落到前端页面中。
- 新增前端直接写数据库的业务核心流程。
- 新增依赖进程内存保存关键状态的长任务逻辑。
- 把 OpenClaw/特定 Agent 能力直接硬编码为整个平台抽象。
- 把部署、容器重启、模型配置等高权限逻辑继续耦合进普通业务接口。

---

## 允许的短期折中
为了保证 MVP 推进速度，以下折中允许存在，但不能继续扩散：
- 少量前端直读 Supabase。
- 单体应用结构。
- 招聘域优先实现。
- 基于现有 OpenClaw 的自动化实现。

前提是：**新增设计必须让未来更容易抽离为平台能力，而不是更难。**

---

## 最终收敛目标
项目最终应收敛为：
- 上层是多个业务域（招聘 / 电商 / 财务）。
- 中层是统一的数字员工运行时（任务、工作流、Agent、事件、审计、调度）。
- 底层是可替换的集成适配器（OpenClaw、ERP、报表系统、IM、邮件等）。

后续所有开发决策，都应服务这个目标。
